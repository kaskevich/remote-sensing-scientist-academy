# Production Geospatial Computing training pack

This CC0-1.0 pack is entirely synthetic and offline. It contains no live credential, private endpoint, protected location or production data. The two-page API fixture supports pagination, duplicate and schema-failure tests without contacting a service.

Use `api_page_1.json`, `api_page_2.json`, `source_contract.json` and `PRODUCTION_WORKFLOW_QA_TEMPLATE.md` to build a recoverable acquisition-to-release workflow. Every source remains read-only; derivatives belong in a separate output directory.
